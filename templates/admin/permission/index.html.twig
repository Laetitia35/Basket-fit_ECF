{% extends 'base.html.twig' %}

{% block title %}Créer les permissions{% endblock %}

{% block body %}
   
   <h3 class="text-center">Créer les permissions</h3>
    
    {{ form_start(form) }}

        {{ form_row(form.Name) }}
        <div class="d-flex justify-content-start">
        {{ form_row(form.Actif) }}
        </div>
        {{ form_row(form.submit) }}

    {{ form_end(form) }}

{% endblock %}
